# OpenSearch Environment
# Copy this file to project root: cp .claude/skills/opensearch-server/assets/.env.example .env

# Project name (used for container, volume, network names)
PROJECT_NAME=myproject

# OpenSearch password (min 8 chars, uppercase, lowercase, digit, special char)
OPENSEARCH_INITIAL_ADMIN_PASSWORD=YourStr0ngP@ss!
